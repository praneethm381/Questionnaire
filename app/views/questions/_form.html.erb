<%= form_for(@question) do |f| %>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text, :size => "50x3" %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>